# Reports

Training logs, GPU traces, generated plots, benchmark outputs, and PDF reports
are generated artifacts and ignored by git.

Publication-ready narrative material belongs in `blog/` or `showcase/`; raw
experiment traces can be regenerated or copied from the artifact root.
