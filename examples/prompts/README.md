# Prompt Examples

These text files are small fixed prompt sets used by local smoke runs,
qualitative sampling, and gate checks.

Pass a custom prompt file with `MCM_GENERATION_PROBE_FILE` or the relevant
module argument.
