# Outputs

Model adapters, tokenizer experiments, checkpoints, and merged model artifacts
are generated here by default and ignored by git.

For large or long-running jobs, set `MCM_ARTIFACT_ROOT` or the stage-specific
output variables to an external data volume such as `/data2/antonio/MariChatmen`.
